

<% @products.each do |product| %>
	<%= link_to image_tag('placeholder.jpg', width: "10%"), product_url(product)%>
  <br>
	<%= product.name %>
	<br>
  price: <a href="<%= product.url %>">$<%= product.price.round %></a>
	<br><br>
<% end %>
